{
  "name": "chatsi-analytics-sdk",
  "version": "1.0.0",
  "description": "Universal e-commerce analytics SDK with PostHog integration",
  "main": "dist/chatsi-analytics-sdk.min.js",
  "scripts": {
    "build": "node build.js && mkdir -p public && cp -r dist/* public/",
    "watch": "node build.js --watch",
    "serve": "npx http-server -p 8080 -c-1"
  },
  "keywords": [
    "analytics",
    "posthog",
    "ecommerce",
    "sfcc",
    "shopify",
    "magento"
  ],
  "author": "Chatsi",
  "license": "MIT",
  "devDependencies": {
    "terser": "^5.27.0"
  }
}
